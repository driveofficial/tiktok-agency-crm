# CLAUDE.md

ไฟล์นี้เก็บ **กฎและข้อเท็จจริงถาวร**ของโปรเจกต์ — สิ่งที่จริงเสมอไม่ว่าวันไหน
สถานะงานปัจจุบัน (ทำถึงไหน ค้างอะไร) อยู่ที่ `docs/HANDOFF.md` ไม่ใช่ที่นี่

## โปรเจกต์นี้คืออะไร

CRM สำหรับเอเจนซี่ที่ชวนครีเอเตอร์ TikTok เข้าร่วมแคมเปญ — แต่ละแถวคือครีเอเตอร์ 1 ราย
รายละเอียดโปรดักต์เต็มอยู่ที่ `PRODUCT.md` อ่านก่อนถ้าจะตัดสินใจเรื่อง UX หรือฟีเจอร์

ผู้ใช้ปลายทางไม่ใช่สายเทค ใช้งานจากมือถือระหว่างวันเป็นหลัก

## โครงสร้าง repo — อ่านก่อนแตะโค้ด

repo นี้มีโค้ด **2 เจนอยู่พร้อมกัน** เพราะกำลังย้ายระบบ ยังไม่จบ:

| | เจนเก่า (Apps Script) | เจนใหม่ (Supabase) |
|---|---|---|
| ไฟล์ | `code.gs`, `App.html`, `Dashboard.html`, `Views.html`, `Modals.html` ฯลฯ ที่ root + `dev/` | `backend/` + `frontend/` |
| สถานะ | **ยังรันใช้งานจริงอยู่** จนกว่าเว็บใหม่จะเสร็จ | กำลังพัฒนา (commit `ad217da` เป็นต้นไป) |

**ความหมายในทางปฏิบัติ:**
- ฟีเจอร์ใหม่ทั้งหมดไปที่ `backend/` + `frontend/` — อย่าเพิ่มของใหม่ลงเจนเก่า
- แต่โค้ดเจนเก่า**ยังห้ามลบและห้ามทำพัง** เพราะทีมยังใช้ทำงานจริงทุกวัน ถ้าจะแก้ต้องรู้ตัวว่ากำลังแตะของที่ live อยู่
- โค้ดเจนเก่าจะถูกเลิกใช้เมื่อเว็บใหม่พร้อมแทนที่ได้ครบ — ยังไม่ถึงจุดนั้น
- **แก้ไฟล์เจนเก่า (`code.gs`/`*.html` ที่ root) แล้วต้อง `cd dev && npm run build` ก่อน commit ด้วย** — `dist/` ถูก track ใน git และเป็นไฟล์ที่ต้อง copy ไป Apps Script editor จริง ถ้าลืม build/commit `dist/` คนที่ deploy รอบหน้าจะเอาโค้ดเก่าไปวางทับ (ไม่มี CI เตือนเหมือนฝั่ง frontend/Vercel)

## ข้อมูลและ sync

- **Google Sheets คือแหล่งข้อมูลตั้งต้น** ทีมยังแก้ข้อมูลที่ชีตโดยตรงทุกวัน — ระบบต้องเคารพว่าชีตคือความจริง
- ข้อมูลกระจายอยู่ใน **หลายไฟล์สเปรดชีต** (1 ไฟล์ = 1 ทีม/แอดมิน) ไม่ใช่ไฟล์เดียวหลายแท็บ — โค้ดที่ assume ไฟล์เดียวจะพังเมื่อทีมเพิ่มขึ้น
- ตาราง `sheets` ใน Supabase เก็บ `spreadsheet_id` + `tab_name` แยกต่อแถว — ใช้ค่านี้เป็นตัวชี้ปลายทางเสมอ อย่า hardcode spreadsheet id ในโค้ด
- Sync ไปกลับสองทางแล้ว (ยืนยันทำงานจริงครบ 4 แท็บ 2026-08-03):
  - **Sheet → Supabase**: `sheet-poll` รันทุกนาทีผ่าน pg_cron เรียก RPC `sync_sheet_records()` (ไม่ใช่ loop ทีละแถวผ่าน PostgREST เหมือนเดิม — เคยชน Google Sheets write quota 429 มาแล้ว)
  - **Supabase → Sheet**: `sheet-push` ยิงจาก trigger บนตาราง `records` (statement-level, `security definer`) ทุกครั้งที่เว็บแอปเขียนข้อมูล — **เขียนทับทั้งแท็บด้วย 1 PUT เสมอ** (ไม่ใช่แก้ทีละเซลล์) แล้ว pad แถวว่างไปถึง 3000 แถวทุกครั้ง (กันข้อมูลเก่าเหลือค้าง) — ผลข้างเคียงที่ตั้งใจ ไม่ใช่บั๊ก
  - **กันวนลูป**: `sync_sheet_records()` ตั้ง `app.skip_sheet_push` ทุกครั้งที่ sheet-poll เขียน กัน sheet-push ยิงย้อนกลับตอน sync ขาเข้า
  - รายละเอียด/ประวัติบั๊กที่เจอ: `docs/superpowers/plans/`
- คอลัมน์ของแต่ละชีตไม่ตายตัวโดยตั้งใจ — อย่ายึดตำแหน่งคอลัมน์ตายตัว ให้อ่าน header ของ sheet นั้นเสมอ
- setup ของ sync ทั้งหมดอธิบายไว้ที่ `backend/README.md`

## Secret — กฎเข้ม

โปรเจกต์นี้มี credential หลายตัวที่หลุดแล้วเสียหายจริง:

- Service account key อยู่นอก repo ที่ `C:\driv\silent-emissary-485208-f8-...json` — ห้ามคัดลอกเข้า repo ห้าม print เนื้อ `private_key`
- `SHEET_SYNC_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, ไฟล์ `.env` — **อย่าขอให้ผู้ใช้พิมพ์ค่าจริงลงในแชท** ให้เขารันคำสั่งเองในเทอร์มินัลโดยส่งเป็น env var แทน
- ก่อน `git add -A` ทุกครั้งให้ดู `git status` ว่ามีไฟล์ credential ปนมาไหม ถ้าเห็น `.json` ที่หน้าตาเหมือน key หรืออะไรที่มีคำว่า key/secret/token ให้หยุดถามก่อน

## การทำงานเป็นทีม (2 คน 2 เครื่อง)

ทีมนี้มี **คิม** และ **โอ๊ค** ทำงานคนละโน้ตบุ๊ก
(อย่าสับสนกับชื่อแท็บในชีต — อาร์ม/ซัน/โอ๊ค/ตี๋น้อย เป็นชื่อทีมในข้อมูล ไม่ใช่คนที่พัฒนาระบบนี้)

บทสนทนา Claude Code เก็บในเครื่องใครเครื่องมัน ข้ามเครื่องไม่ได้ — **git repo คือช่องทางเดียวที่ context จะถึงอีกคน**

**ตั้งค่าครั้งแรกในเครื่องใหม่** (ทำครั้งเดียวหลัง clone) — ไม่ตั้งแล้ว commit จะไปโผล่ในชื่อคนอื่นหรือ commit ไม่ได้เลย:

```bash
git config user.name "<ชื่อตัวเอง>"     # คิม หรือ โอ๊ค
git config user.email "<อีเมลตัวเอง>"
```

ใช้แบบไม่ใส่ `--global` เพื่อให้มีผลเฉพาะ repo นี้

- **เริ่มงานพิมพ์ `/start` จบงานพิมพ์ `/end`** — สองคำสั่งนี้เรียก skill `session-handoff` ให้ pull + อ่านสถานะตอนเริ่ม และอัปเดต `docs/HANDOFF.md` + push ตอนจบ (คำสั่งอยู่ที่ `.claude/commands/`, สกิลอยู่ที่ `.claude/skills/session-handoff/`)
- **ทำงานบน `main` ตรงๆ ไม่ต้องแตก branch** — ทีมมี 2 คน การเพิ่มพิธีกรรม branch/PR เป็นต้นทุนที่ยังไม่คุ้มในตอนนี้
- `git pull` ก่อนเริ่มเสมอ ถ้าเจอ conflict ให้หยุดถามผู้ใช้ อย่า resolve เองเงียบๆ — งานที่ยังไม่ commit ของอีกคนหายได้
- ถ้าจะแก้ไฟล์เดียวกันในเวลาไล่เลี่ยกัน บอกกันก่อน — ไม่มีระบบจองงาน ใช้การคุยกันเอา

## Deploy — ต้องรู้ก่อน push

**push เข้า `main` = Vercel deploy เว็บใหม่ขึ้น production ทันที** ไม่มีขั้นตอนกดยืนยัน

ผลที่ตามมาในทางปฏิบัติ:

- ก่อน commit ถ้ารอบนี้แตะไฟล์ใน `frontend/` ให้รัน `cd frontend && npm run build` ก่อน — ถ้า build ไม่ผ่านให้หยุดแก้ก่อน อย่า push
- ตาข่ายกันตก: Vercel จะไม่ deploy งานที่ build ไม่ผ่าน เวอร์ชันเดิมจะยังอยู่ — เว็บไม่ล่มจาก syntax error แต่**บั๊กที่ build ผ่านจะขึ้นจริง**
- ถ้า deploy แล้วพบว่าพัง ย้อนกลับได้ที่หน้า Vercel (rollback ไปเวอร์ชันก่อนหน้า) เร็วกว่าแก้โค้ดแล้ว push ใหม่
- โค้ดเจนเก่า (Apps Script) **ไม่เกี่ยวกับ Vercel** — ต้องคัดลอกไฟล์ไป deploy ในโปรเจกต์ Apps Script เองแยกต่างหาก

**เมื่อไหร่ต้องกลับมาทบทวนเรื่องนี้:** เมื่อทีมเริ่มใช้เว็บใหม่ทำงานจริง การ push โค้ดที่ยังไม่เสร็จจะกระทบคนทำงานทันที ถึงตอนนั้นค่อยแยก branch หรือย้ายให้ Vercel deploy จาก branch `production` แทน `main`

## Flow ของงานหนึ่งชิ้น

1. **`/start`** — pull งานล่าสุด อ่าน `docs/HANDOFF.md` แล้วเลือกงานจากหัวข้อ "ทำอะไรต่อ"
2. **งานใหญ่หลาย step → เขียนแผนก่อน** ลงใน `docs/superpowers/plans/` เป็น checkbox แล้วค่อยลงมือ
   งานเล็กจบในรอบเดียว → ทำเลย ไม่ต้องเขียนแผน
   เหตุผลที่ต้องมีแผนสำหรับงานใหญ่: อีกคนเห็นได้ว่างานเดินถึง step ไหน และถ้าทำค้างข้ามวันจะหยิบต่อได้โดยไม่ต้องเดา
3. **ทำงาน** — ดูผลจริงด้วย `cd frontend && npm run dev` ไม่ใช่เดาจากโค้ด
4. **`/end`** — ติ๊ก checkbox ที่ทำเสร็จ อัปเดต HANDOFF เช็ค build เช็ค secret แล้ว commit + push

ระหว่างทาง ถ้าเจอข้อเท็จจริงถาวรที่ยังไม่มีใครเขียนไว้ (เช่น "ตารางนี้ใช้ยังไง", "อย่าแตะไฟล์นี้เพราะ...") ให้เขียนลง `CLAUDE.md` เลย อย่าปล่อยให้อยู่แค่ในบทสนทนา เพราะอีกเครื่องจะไม่มีทางรู้

## คำสั่งที่ใช้บ่อย

```bash
# frontend (เจนใหม่)
cd frontend && npm run dev        # dev server
cd frontend && npm run build      # build production

# เจนเก่า (Apps Script) — preview/build/test ในเครื่อง
cd dev && npm run preview
cd dev && npm run build
cd dev && npm test
```

Supabase migrations อยู่ที่ `backend/supabase/migrations/` และ edge functions ที่ `backend/supabase/functions/`
วิธี deploy และตั้ง secret อธิบายไว้ใน `backend/README.md`

## ธรรมเนียมการสื่อสาร

- **ตอบเป็นภาษาไทย** ทีมนี้สื่อสารกันเป็นไทยทั้งหมด รวมถึง commit message, เอกสารใน `docs/`, และคอมเมนต์ในโค้ดส่วนที่อธิบาย business logic
- ชื่อฟังก์ชัน/ตัวแปรยังเป็นอังกฤษตามปกติ
