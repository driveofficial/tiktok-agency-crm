-- ============================================================================
-- 0008b_drop_webhook_secret.sql — ยกเลิก 0008 (เก็บ secret เป็น Edge Function
-- secret env var แทน ไม่ใช้ตาราง DB)
-- ============================================================================

drop table if exists webhook_secret;
