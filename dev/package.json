{
  "name": "tiktok-crm-dev",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dev tooling for TikTok Agency CRM (Apps Script). Local preview, JSX precompile, tests.",
  "scripts": {
    "preview": "node preview.mjs",
    "build": "node build.mjs",
    "test": "node --test \"tests/*.test.mjs\""
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
