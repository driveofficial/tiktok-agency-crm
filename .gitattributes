# Normalize line endings to LF in the repo (Windows editors keep flipping to CRLF).
# Prevents whole-file "phantom" diffs when only a few lines really changed.
* text=auto eol=lf
*.png binary
*.jpg binary
*.gif binary
